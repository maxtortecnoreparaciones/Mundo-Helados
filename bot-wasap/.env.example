# .env.example - Copy to .env and fill values (do NOT commit .env)
# Gemini API key for AI calls
GEMINI_API_KEY=

# Google service account (base64 JSON or path to service_account.json depending on your deployment)
GOOGLE_SERVICE_ACCOUNT=

# Any other secrets used by the project
OTHER_SECRETS=

# Node environment
NODE_ENV=development
